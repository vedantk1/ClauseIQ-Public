# Web framework and request handling
fastapi==0.141.1
starlette==1.6.0
uvicorn==0.52.4
python-multipart==0.0.32
pydantic==2.13.5
pydantic-settings==2.15.0

# Database
pymongo==4.18.0
motor==3.7.1

# Local credential encryption
cryptography==50.0.1
certifi==2026.7.22

# PDF extraction and report generation
pdfplumber==0.11.10
reportlab==5.0.1
# Pin the existing PDF imaging dependency for reproducible raster test fixtures.
pillow==12.3.0

# AI and vector search
# Stay on the latest compatible OpenAI Python 1.x release until a deliberate
# SDK v2 migration is tested against the application's request paths.
openai==1.109.1
tiktoken==0.14.0
# Tested with the pinned Compose server; review both versions together (DOCKER.md).
qdrant-client==1.16.2
numpy==2.5.2

# Configuration and diagnostics
python-dotenv==1.2.3
psutil==7.2.2

# Tests
pytest==9.1.1
pytest-asyncio==1.4.0

# Local shared types package (editable install for single-dev workflow)
-e ../shared
